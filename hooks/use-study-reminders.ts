// Este arquivo existe apenas para satisfazer a resolução de módulo do TypeScript.
// O Metro Bundler usa automaticamente .native.ts (iOS/Android) ou .web.ts (web) em runtime.
export function useStudyReminders(_isLoggedIn: boolean): void {
  // no-op – substituído por .native.ts ou .web.ts em runtime
}
