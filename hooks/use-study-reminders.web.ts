/**
 * Stub para web — notificações locais não são suportadas.
 * O Metro carrega este arquivo em vez de .native.ts na plataforma web.
 */
export function useStudyReminders(_isLoggedIn: boolean): void {
  // no-op on web
}
